<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ku.edu.af.classapplication">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReadingDetailActivity" />
        <activity android:name=".TerminologyActivity" />
        <activity android:name=".TermDetailsActivity" />
        <activity android:name=".AddReadingActivity" />
        <activity android:name=".AddTerminologyActivity" />
        <activity android:name=".CourseActivity" />
        <activity android:name=".ReadingActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".LecturesActivity" />
        <activity android:name=".PostDetailActivity" />
        <activity android:name=".FileActivity"></activity>
    </application>

</manifest>